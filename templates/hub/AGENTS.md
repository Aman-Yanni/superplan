# @@HUB_NAME@@ — Agent notes

This hub is data-only. Follow `CLAUDE.md` (repo rules first, never merge by
default). Superplan skills are installed globally, not copied here.

Cursor: add each bound product repo to the workspace. Claude Code: bound repos
are listed in `.claude/settings.json` `permissions.additionalDirectories`.
