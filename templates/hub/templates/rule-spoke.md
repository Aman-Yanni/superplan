# <Area> — hub spoke

Adds knowledge on top of the repo's own rules. It never replaces them.

## Sources of truth (read first, in order)

1. `<path>` — <what it governs>

## Verified facts

- <fact>. Evidence: `<path>`.
  <!-- last-verified: YYYY-MM -->
