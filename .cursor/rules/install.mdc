---
description: Superplan install.sh — agent select, symlink default, copy opt-in
globs: "install.sh,tests/**/*.sh"
alwaysApply: false
---

# Installer Rules

## Scope

What this spoke covers:

- `./install.sh` (humanize-style): pick agents, symlink or `--copy`
- Destinations for Claude Code and Cursor
- Authoring vs shipped install

What this spoke does **not** cover (route elsewhere):

- `npx skills add` → `skills-cli.mdc`
- What a skill file contains → `agent-skills.mdc`

## Invariants

1. Default is **symlink** from this repo's `skills/<name>` into each selected
   agent's global skills dir, so `git pull` updates are live while authoring.
   Dest paths must use `"$HOME/.claude/skills"` and `"$HOME/.cursor/skills"`,
   never `~` (tilde ignores a faked `HOME`).
2. Supported install targets at minimum: `claude` → `$HOME/.claude/skills`,
   `cursor` → `$HOME/.cursor/skills`. `all` (or interactive pick) does both.
   Interactive walkthrough is the no-args path (like humanize, plus Cursor).
3. `--copy` copies instead of linking. Say so in the output. Do not `rm -rf`
   a destination that is not a Superplan skill without asking. A dest is ours
   if it is a symlink to this pack's `skills/<name>`, or a directory whose
   `.superplan-install` first line is that absolute source path. Preflight
   every dest before applying any.
4. `./install.sh` must work with **no Node**. `npx skills` is the later
   distribution path, not a requirement for local install.
5. Tests run against a fake `HOME`, never the developer's real skill dirs,
   unless the user explicitly asked to install on this machine.
6. Reference shape: https://github.com/harshaneel/humanize/blob/main/install.sh

## Docs (if stuck):

**Docs (if stuck):** https://github.com/harshaneel/humanize/blob/main/install.sh

- Prefer official docs over blog posts. Date-stamp when consulted.
- last consulted: 2026-09

Also: Cursor dest https://cursor.com/docs/skills · Claude dest
https://code.claude.com/docs/en/skills

## Problem classes

### `Copy during authoring`

| Symptom | Cause | Fix |
| ------- | ----- | --- |
| Edits in the Superplan repo do not show up in `/grill-me` | Install used `--copy` (or npx `--copy`) | Reinstall as symlink; default `install.sh` to symlink |

<!-- last-verified: 2026-09 -->

### `Installer wiped a foreign skill`

| Symptom | Cause | Fix |
| ------- | ----- | --- |
| Unrelated skill directory gone after install | `rm -rf` on a dest name collision | Only replace dest if it is a Superplan symlink (readlink equals pack `skills/<name>`) or a previous Superplan copy (`.superplan-install` marker). Preflight all dests; otherwise stop and ask |

<!-- last-verified: 2026-09 -->

### `Tilde ignores fake HOME`

| Symptom | Cause | Fix |
| ------- | ----- | --- |
| Tests install into the real home, or a faked `HOME` stays empty | Script used `~/.claude` / `~/.cursor` (passwd home), not `$HOME` | Use `"$HOME/.claude/skills"` and `"$HOME/.cursor/skills"` everywhere |

<!-- last-verified: 2026-09 -->
